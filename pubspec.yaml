name: hadith_app
description: Islamic Companion App
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  intl: ^0.19.0
  adhan: ^2.0.0

flutter:
  uses-material-design: true
